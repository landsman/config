# Never symlink these into $HOME. Stow ignores `^/README.*` by default, but a
# local ignore file replaces the whole default list, so the usual entries are
# repeated here rather than assumed.
^/README.*
^/\.stow-local-ignore$
\.git
\.gitignore
.+~
\#.*\#
